# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies and build output
node_modules/
dist/
dist-ssr/
coverage/
*.local

# Local-only build of the live-demo app embedded by the landing page.
# CI (deploy-pages.yml) regenerates this into the Pages artifact; never commit it.
page/demo/

# Rust / Tauri build artifacts
target/
src-tauri/gen/schemas/*.json
src-tauri/WixTools/
src-tauri/.cargo/

# Audit and test report output
test-results-raw.json
*.tsbuildinfo

# Environment and secrets
.env
.env.*
!.env.example

# Tauri signing keys (NEVER commit these)
~/.tauri/
*.key
*.key.pub

# Editor and IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# Temporary files
*.bak
*.tmp
*.swp
*.orig

# Agent workspace
.agents
~
analytics